<div class="row product-grid">
    <isloop items="${pdict.productSearchCategory}" var="product">  
        <div class="col-6 col-sm-3">  
            <isinclude url="${URLUtils.url('Tile-Show', 'pid', product.ID, 'swatches', true, 'ratings', true)}" />
        </div>
    </isloop>
</div>
